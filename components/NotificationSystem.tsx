import React from 'react';

const NotificationSystem: React.FC = () => {
  return (
    <div id="notification-container" className="fixed top-4 right-4 z-50 space-y-2">
      {/* 알림들이 여기에 동적으로 추가됩니다 */}
    </div>
  );
};

export default NotificationSystem;
